# Руководство для AI-агентов

## 🚨 КРИТИЧЕСКИ ВАЖНО: Обязательный порядок работы

**ПЕРЕД НАЧАЛОМ ЛЮБОЙ РАБОТЫ** вы ДОЛЖНЫ выполнить следующие шаги в строгом порядке:

### Шаг 1: Прочитать README.md
Начните с полного прочтения файла `README.md` в корне проекта. Этот файл содержит:
- Общее описание проекта
- Его назначение и цели
- Базовую структуру
- Информацию о запуске

### Шаг 2: Изучить ВСЮ папку docs/
**ОБЯЗАТЕЛЬНО** прочитайте **ПОЛНОСТЬЮ** все файлы в папке `docs/`:
- Изучите каждый файл от начала до конца

### Шаг 3: Только теперь приступайте к работе
После **полного** изучения документации вы можете начинать выполнение задачи.

---

## 📋 Принципы работы

### 1. Систематический подход
- **Не начинайте кодировать сразу** - сначала поймите контекст
- Изучите существующую структуру проекта перед внесением изменений
- Проверьте наличие похожих решений в кодовой базе
- Следуйте установленным паттернам и конвенциям проекта

### 2. Чтение кода перед изменением
- Всегда читайте существующий код перед модификацией
- Понимайте зависимости между компонентами
- Ищите существующие утилиты и функции для переиспользования
- Избегайте дублирования функциональности

### 3. Использование инструментов
- `codebase_search` - для семантического поиска по смыслу
- `grep` - для точного поиска по тексту
- `read_file` - для чтения файлов полностью
- `search_replace` - для редактирования существующих файлов
- Параллельные вызовы инструментов для повышения эффективности

---

## 📝 ОБЯЗАТЕЛЬНОЕ обновление документации

### Когда НЕОБХОДИМО обновить docs/

Вы **ДОЛЖНЫ** обновить документацию в папке `docs/`, если вносите изменения типа:

#### ✅ Значительные изменения (требуют обновления документации):

1. **Изменение основных пользовательских потоков**
   - Модификация процесса регистрации/аутентификации
   - Изменение основного flow взаимодействия с ботом
   - Перестройка процесса обработки команд

2. **Архитектурные изменения**
   - Добавление новых слоев или модулей
   - Изменение структуры базы данных
   - Внедрение новых паттернов проектирования
   - Изменение взаимодействия между компонентами

3. **Новая ключевая функциональность**
   - Добавление нового типа транзакций
   - Внедрение системы категорий
   - Добавление аналитики и отчетов
   - Интеграция с внешними API

4. **Изменение конфигурации и окружения**
   - Новые переменные окружения
   - Изменение процесса деплоя
   - Добавление новых сервисов в docker-compose

5. **Изменение API или интерфейсов**
   - Модификация существующих endpoints
   - Изменение формата команд бота
   - Изменение структуры ответов

#### ❌ Незначительные изменения (не требуют обновления документации):

- Добавление новой библиотеки без изменения архитектуры
- Исправление багов
- Рефакторинг без изменения функциональности
- Оптимизация производительности без изменения API
- Обновление зависимостей

### Как обновлять документацию

1. **Создайте или обновите соответствующий .md файл** в `docs/`
2. **Опишите изменения четко и структурировано**
3. **Добавьте примеры использования** если применимо
4. **Обновите диаграммы** если изменилась архитектура
5. **Укажите дату изменения** и краткое описание

### Структура документации

```
docs/
├── AGENTS.md           # Это руководство для агентов
├── ARCHITECTURE.md     # Архитектура проекта (создать при необходимости)
├── API.md             # Описание API и команд (создать при необходимости)
├── DATABASE.md        # Схема БД (создать при необходимости)
└── DEPLOYMENT.md      # Процесс деплоя (создать при необходимости)
```

---

## 🎯 Процесс выполнения задачи

### 1. Анализ задачи
```
□ Прочитал README.md
□ Изучил все файлы в docs/
□ Понял требования задачи
□ Определил, какие компоненты затронуты
□ Проверил существующий код
```

### 2. Планирование
```
□ Создал TODO список для сложных задач
□ Определил последовательность шагов
□ Выявил зависимости
□ Понял, нужно ли обновлять документацию
```

### 3. Реализация
```
□ Следую установленным паттернам
□ Пишу чистый, читаемый код
□ Добавляю комментарии для сложной логики
□ Использую существующие утилиты
```

### 4. Проверка
```
□ Проверил линтер (если есть)
□ Убедился, что код работает
□ Обновил документацию (если нужно)
□ Проверил, что не сломал существующую функциональность
```

---

## 💡 Лучшие практики для агентов

### Коммуникация
- Объясняйте свои действия четко и кратко
- Используйте структурированный вывод (списки, заголовки)
- Показывайте код в правильном формате (code references для существующего, markdown для нового)
- Не упоминайте названия инструментов, говорите естественным языком

### Эффективность
- Используйте параллельные вызовы инструментов когда возможно
- Не читайте файлы повторно без необходимости
- Ищите информацию семантически перед чтением больших файлов
- Начинайте с широкого поиска, затем сужайте область

### Качество кода
- Следуйте существующему стилю кода
- Не создавайте временные файлы без удаления в конце
- Предпочитайте редактирование существующих файлов созданию новых
- Используйте правильные инструменты для правильных задач

### Управление задачами
- Создавайте TODO для сложных задач (3+ шага)
- Обновляйте статус в реальном времени
- Завершайте задачи перед началом новых
- Не оставляйте задачи незавершенными

---

## 🔍 Поиск и навигация

### Когда использовать codebase_search
- "Как работает аутентификация?"
- "Где обрабатываются команды бота?"
- "Какие есть модели данных?"

### Когда использовать grep
- Точный поиск текста или символов
- Поиск импортов конкретного класса
- Поиск всех использований функции

### Когда использовать read_file
- Чтение известных файлов целиком
- Изучение небольших файлов
- Просмотр конфигурационных файлов

---

## ⚠️ Важные напоминания

1. **ВСЕГДА** читайте README.md и docs/ ПЕРЕД началом работы
2. **ОБЯЗАТЕЛЬНО** обновляйте документацию при значительных изменениях
3. **НЕ СОЗДАВАЙТЕ** файлы документации (README, CHANGELOG) без запроса
4. **СЛЕДУЙТЕ** существующим паттернам проекта
5. **ИСПОЛЬЗУЙТЕ** семантический поиск для понимания кода
6. **ЗАВЕРШАЙТЕ** все TODO перед окончанием работы

---

## Система памяти AICODE (Context Anchors)
Эта система критически важна для сохранения контекста между сессиями. Агенты имеют ограниченную память, поэтому вы ОБЯЗАНЫ оставлять "хлебные крошки" прямо в коде.

Правило: Если вы принимаете неочевидное решение, оставляете недоделку или видите потенциальную проблему — вы обязаны оставить соответствующий AICODE-комментарий.

Используйте формат: # AICODE-<ТИП>: <сообщение>

Типы анкоров:
1. AICODE-NOTE: — Архитектурное решение. Объясните, почему сделано именно так.
Пример: "Используем здесь polling вместо webhook, так как на dev-среде нет белого IP."
Когда писать: При выборе неочевидного алгоритма, использовании "хаков" или временных решений.

2. AICODE-TODO: — Технический долг. Что нужно сделать, но не сейчас.
Пример: "Нет валидации входящего JSON, добавить позже."
Когда писать: При использовании pass, заглушек (stub), хардкода, отсутствии обработки edge-cases или отсутствии тестов.
Rule of Thumb: Если вы пишете код для MVP, задайте себе вопрос: "Что сломается или будет неудобно, если это выкатить на 10 000 пользователей?" Если ответ есть (нет кнопки отмены, нет лимитов на ввод, хардкод текстов) — обязательно ставьте AICODE-TODO.

3. AICODE-QUESTION: — Блокер/Вопрос.
Пример: "Неясно, как обрабатывать отмену подписки. Уточнить у PM."
Пример использования в коде:

```
# AICODE-NOTE: Используем глобальную переменную для кэша ради MVP.
# В продакшене заменить на Redis.
user_cache = {}

async def get_user(user_id):
    # AICODE-TODO: Добавить обработку ошибок сети
    if user_id in user_cache:
        return user_cache[user_id]
```